apiVersion: apps/v1
kind: Deployment
metadata:
  name: orders-v1
  namespace: ${NAMESPACE:-default}
  labels:
    app: orders
    version: v1
    environment: ${ENVIRONMENT:-dev}
spec:
  replicas: ${ORDERS_V1_REPLICAS:-1}
  selector:
    matchLabels:
      app: orders
      version: v1
  template:
    metadata:
      labels:
        app: orders
        version: v1
        environment: ${ENVIRONMENT:-dev}
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "5001"
        prometheus.io/path: "/metrics"
    spec:
      containers:
      - name: orders
        image: ${IMAGE_REGISTRY:-docker.io/noletengine}/stock-orders:${IMAGE_TAG:-latest}
        ports:
        - containerPort: 5001
        env:
        - name: SERVICE_VERSION
          value: "v1"
        - name: PORT
          value: "5001"
        - name: ENVIRONMENT
          value: "${ENVIRONMENT:-dev}"
        livenessProbe:
          httpGet:
            path: /health
            port: 5001
          initialDelaySeconds: ${LIVENESS_DELAY:-30}
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 5001
          initialDelaySeconds: ${READINESS_DELAY:-5}
          periodSeconds: 5
        resources:
          requests:
            memory: "${SERVICE_MEMORY_REQUEST:-64Mi}"
            cpu: "${SERVICE_CPU_REQUEST:-50m}"
          limits:
            memory: "${SERVICE_MEMORY_LIMIT:-128Mi}"
            cpu: "${SERVICE_CPU_LIMIT:-100m}"

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: orders-v2
  namespace: ${NAMESPACE:-default}
  labels:
    app: orders
    version: v2
    environment: ${ENVIRONMENT:-dev}
spec:
  replicas: ${ORDERS_V2_REPLICAS:-0}
  selector:
    matchLabels:
      app: orders
      version: v2
  template:
    metadata:
      labels:
        app: orders
        version: v2
        environment: ${ENVIRONMENT:-dev}
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "5001"
        prometheus.io/path: "/metrics"
    spec:
      containers:
      - name: orders
        image: ${IMAGE_REGISTRY:-docker.io/noletengine}/stock-orders:${IMAGE_TAG:-latest}
        ports:
        - containerPort: 5001
        env:
        - name: SERVICE_VERSION
          value: "v2"
        - name: PORT
          value: "5001"
        - name: ENVIRONMENT
          value: "${ENVIRONMENT:-dev}"
        livenessProbe:
          httpGet:
            path: /health
            port: 5001
          initialDelaySeconds: ${LIVENESS_DELAY:-30}
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 5001
          initialDelaySeconds: ${READINESS_DELAY:-5}
          periodSeconds: 5
        resources:
          requests:
            memory: "${SERVICE_MEMORY_REQUEST:-64Mi}"
            cpu: "${SERVICE_CPU_REQUEST:-50m}"
          limits:
            memory: "${SERVICE_MEMORY_LIMIT:-128Mi}"
            cpu: "${SERVICE_CPU_LIMIT:-100m}"